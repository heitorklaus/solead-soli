import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:login/app/modules/simulatorPlant/simulator_plant_page.dart';

void main() {
  testWidgets('SimulatorPlantPage has title', (tester) async {
  //  await tester.pumpWidget(buildTestableWidget(SimulatorPlantPage(title: 'SimulatorPlant')));
  //  final titleFinder = find.text('SimulatorPlant');
  //  expect(titleFinder, findsOneWidget);
  });
}
  