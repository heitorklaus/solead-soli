import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:login/app/modules/update/update_page.dart';

void main() {
  testWidgets('UpdatePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UpdatePage(title: 'Update')));
    //  final titleFinder = find.text('Update');
    //  expect(titleFinder, findsOneWidget);
  });
}
