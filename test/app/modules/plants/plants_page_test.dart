import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:login/app/modules/plants/plants_page.dart';

void main() {
  testWidgets('PlantsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PlantsPage(title: 'Plants')));
    //  final titleFinder = find.text('Plants');
    //  expect(titleFinder, findsOneWidget);
  });
}
