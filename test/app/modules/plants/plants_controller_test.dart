import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:login/app/modules/plants/plants_controller.dart';
import 'package:login/app/modules/plants/plants_module.dart';

void main() {
  initModule(PlantsModule());
  // PlantsController plants;
  //
  setUp(() {
    //     plants = PlantsModule.to.get<PlantsController>();
  });

  group('PlantsController Test', () {
    //   test("First Test", () {
    //     expect(plants, isInstanceOf<PlantsController>());
    //   });

    //   test("Set Value", () {
    //     expect(plants.value, equals(0));
    //     plants.increment();
    //     expect(plants.value, equals(1));
    //   });
  });
}
