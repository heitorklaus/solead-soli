import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:login/app/modules/simulator/simulator_page.dart';

main() {
  testWidgets('SimulatorPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SimulatorPage(title: 'Simulator')));
    //  final titleFinder = find.text('Simulator');
    //  expect(titleFinder, findsOneWidget);
  });
}
