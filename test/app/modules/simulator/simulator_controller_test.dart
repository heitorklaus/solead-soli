import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:login/app/modules/simulator/simulator_controller.dart';
import 'package:login/app/modules/simulator/simulator_module.dart';

void main() {
  initModule(SimulatorModule());
  // SimulatorController simulator;
  //
  setUp(() {
    //     simulator = SimulatorModule.to.get<SimulatorController>();
  });

  group('SimulatorController Test', () {
    //   test("First Test", () {
    //     expect(simulator, isInstanceOf<SimulatorController>());
    //   });

    //   test("Set Value", () {
    //     expect(simulator.value, equals(0));
    //     simulator.increment();
    //     expect(simulator.value, equals(1));
    //   });
  });
}
