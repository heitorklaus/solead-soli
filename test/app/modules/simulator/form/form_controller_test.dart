import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:login/app/modules/simulator/simulator_module.dart';

void main() {
  initModule(SimulatorModule());
  // FormController form;
  //
  setUp(() {
    //     form = SimulatorModule.to.get<FormController>();
  });

  group('FormController Test', () {
    //   test("First Test", () {
    //     expect(form, isInstanceOf<FormController>());
    //   });

    //   test("Set Value", () {
    //     expect(form.value, equals(0));
    //     form.increment();
    //     expect(form.value, equals(1));
    //   });
  });
}
